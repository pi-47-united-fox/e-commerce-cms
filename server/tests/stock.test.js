const request = require('supertest')
const app = require('../app.js')

let access_token_admin 
let access_token_cust 
let stock_id

//beforeAll hooks untuk mendapat access_token_admin
beforeAll(done => {

})

// afterAll hook untuk menghapus data di tabel Stocks
afterAll(done => {
    
})
